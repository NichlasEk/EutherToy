[id]
name = "debian"
label = "Debian Installer"
type = "linux-installer"

[match]
filename_contains = ["debian", "netinst"]

[boot]
kernel = "/extract/debian/install.amd/vmlinuz"
initrd = "/extract/debian/install.amd/initrd.gz"
args = [
  "auto=false",
  "priority=low"
]
